# Railway Configuration - Unified Container
# Un solo servicio con todos los componentes (frontend + 3 backends)

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.unified"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
